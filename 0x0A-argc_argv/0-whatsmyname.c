#include <stdio.h>

/**
 * main - Program that prints itself followed by a new line
 * @argc: number of command line arguments
 * @argv: Array or arguments
 * Return: 0
 */

int main(int argc, char **argv)
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
